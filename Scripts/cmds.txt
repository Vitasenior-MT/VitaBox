#mongo backups
mkdir mongobackups
mongodump --db VitaBoxDev2 --out mongobackups/`date +"%m-%d-%y"`
#mongo restoring
mongorestore --db VitaBoxDev2 --drop mongobackups/01-20-16/VitaBoxDev2/

#check for disk space in raspbian
df -Bm or df -h

#settimezone
sudo timedatectl set-timezone Europa/Lisboa

